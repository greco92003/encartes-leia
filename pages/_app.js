// Este arquivo serve como configuração da aplicação para o Vercel
import React from 'react';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
